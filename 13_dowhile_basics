#include <stdio.h>
int main()
{
    int n , avg, i=1,sum=0;
    printf("/n enter number");
    scanf("%d",&n);
    do
    {
       sum=sum+i;
       i=i+1 ;
    }while(i<=n);
    avg=sum/n;
    printf("\n average = %d",avg);

}
